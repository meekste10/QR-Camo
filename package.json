{
  "name": "qr-camo",
  "version": "0.6.3",
  "private": true,
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && mkdir -p dist/js dist/assets && cp index.html dist/index.html && cp styles.css dist/styles.css && cp -R js/. dist/js && cp -R assets/. dist/assets",
    "sync": "npm run build && npx cap sync ios",
    "open:ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  }
}
